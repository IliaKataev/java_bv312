1. Банк - содержит коллекции для хранения клиентов, счетов, операций над счетами.
2. Счет Account - Счет в банке. Конструктор был приватный. Экземпляр класса нельзя создать (вне банка). Счет создать можно только с помощью метода класса Банк.
Если счет создается повторно для существующего клиента, то повторно объект клиента создаться не будет
3. Клиент - клиент банка
4. Операции в банке
5. Операции со счетом
6. Операции переводов денежных средств
7. Класс для описания исключительных ситуаций

Нужно сделать единую централизованную систему логирования ошибок

1. Ошибка должна храниться как:
   * final Дата
   * final Сообщение

 2. Должна быть возможность добавлять ошибку из любого места программы
 3. Унификация обработки ошибок
 (singleton + logger)

 1.1 BankError - модель ошибки
    getDate, getMessage toString
 4. ErrorLogger - единый журнал ошибок (singleton) (private static final ErrorLogger INSTANCE = new ErrorLogger();)
    getInstance() - return INSTANCE
    log - add(new BankError(message)
    getErrors(), printErrors()
